# Roadmap

Status: Not initialized. Run `prompts/START_NEW_PROJECT.md`.

Once initialized, this file carries the phase list and the canonical current-phase
marker (`[>]`). See `docs/system/TOKEN_EFFICIENCY.md` and `templates/ROADMAP.template.md`.
